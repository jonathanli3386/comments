[project]
name = "comments-backend"
version = "0.1.0"
description = "RESTful comments API built with Django REST Framework"
requires-python = ">=3.11"
dependencies = [
    "django>=5.0,<6.0",
    "djangorestframework>=3.15",
    "django-cors-headers>=4.4",
    "python-dotenv>=1.0",
]
